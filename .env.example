# For local development, use a local PostgreSQL instance:
# For Render deployment, use the External Database URL from your Render PostgreSQL instance
DATABASE_URL=postgresql://<USER>:<PASSWORD>@localhost:5432/currency_db?schema=public
PORT=3000
NODE_ENV=production
